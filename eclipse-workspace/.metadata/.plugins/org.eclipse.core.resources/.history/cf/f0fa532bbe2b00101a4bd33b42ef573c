<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Bài 1 - HTML CSS</title>
    
    <!-- Nhúng Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* Class căn giữa chữ */
        .center-text {
            text-align: center;
        }

        /* Class chuyển chữ thành chữ hoa */
        .uppercase {
            text-transform: uppercase;
        }

        /* Class đổi màu chữ */
        .text-red {
            color: red;
        }

        /* Class đổi màu nền */
        .bg-yellow {
            background-color: yellow;
        }
    </style>
</head>
<body>

    <!-- Họ tên trong thẻ h1 -->
    <h1 class="center-text uppercase text-red bg-yellow">Nguyễn Văn A</h1>

</body>
</html>
